import 'dart:async';

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('adds one to input values', () {
    Timer(Duration(seconds: 5), () {
      print("sss-sss");
    }).cancel();
  });
}
